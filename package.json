{
  "name": "marketing-analytics-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix frontend run preview -- --host 0.0.0.0 --port $PORT",
    "preview": "npm --prefix frontend run preview -- --host 0.0.0.0 --port $PORT",
    "dev": "npm --prefix frontend run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
